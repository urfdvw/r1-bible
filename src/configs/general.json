{
    "name": "general",
    "title": "通用",
    "type": "object",
    "properties": {
        "theme": {
            "title": "主题",
            "type": "string",
            "enum": ["跟随系统", "白天", "夜间", "夜间投影"],
            "default": "跟随系统"
        },
        "show_tips_on_startup": {
            "title": "在启动时显示贴士",
            "type": "string",
            "enum": ["是", "否"],
            "default": "是"
        }
    }
}
